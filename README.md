# Истина · Анализ продаж

Еженедельный отчёт по продажам кухни и бара. Один файл, открывается двойным
кликом — ни установки, ни интернета, ни сервера.

## Начать

Скачайте **`отчет.html`** и откройте в браузере. Данные уже внутри —
19 недель, с 4 мая по 13 сентября 2026, — отчёт открывается сразу с цифрами.

## Если отчёт присылают готовым

Файл `отчет.html` можно просто заменить на новый — накопленное в браузере не
пропадёт. В файл вшита версия данных; когда она меняется, отчёт при открытии
обновляет всё, что пришло из прошлых версий файла, и не трогает то, что вы
загружали сами через вкладку «Данные».

Если цифры на экране не совпадают с вашими таблицами, проверьте счётчик недель
в шапке: он должен совпадать с тем, что написано в этом README. Не совпадает —
открыт старый файл.

Поэтому раздать обновление коллегам можно, просто положив новый `отчет.html`
в общую папку.

## Раз в неделю

1. Вкладка **«Данные»**.
2. Перетащите обе выгрузки — по кухне и по бару. Период и раздел определяются
   по шапке файла, можно бросать сразу несколько недель.
3. Дашборд и «Категории» пересчитаются сами.
4. Там же, внизу, нажмите **«Экспортировать базу»** и положите файл в облако.

Четвёртый шаг — не формальность. База лежит в браузере на этом компьютере:
на другом её нет, чистка истории браузера её удаляет, при смене ноутбука она
не переедет. Файл базы — единственный способ перенести работу и поделиться ею
с коллегами (у них: «Импортировать базу»).

## Общая база с коллегами

Отчёт работает без сервера, поэтому сам разослать изменения он не может:
браузер не умеет писать на чужой компьютер. Нужен общий носитель — и без базы
данных это папка в облаке (Яндекс.Диск, OneDrive, Google Диск).

Порядок работы:

1. Тот, кто загрузил свежие выгрузки, нажимает **«Выгрузить базу»** — скачается
   файл `истина-база.json`.
2. Кладёт его в общую папку поверх старого. Имя всегда одинаковое, поэтому файл
   просто заменяется.
3. Коллега открывает отчёт и нажимает **«Загрузить базу»** (или просто
   перетаскивает файл в поле загрузки выгрузок).

Импорт **объединяет**, а не затирает. Недели независимы, поэтому конфликтов
почти не бывает: чего у вас нет — добавится, что есть у обоих — останется в
той версии, которая записана позже. Перед объединением отчёт показывает
сводку: сколько разделов добавится, сколько обновится, сколько ваших останется.

Так двое могут работать параллельно: один заливает кухню, другой бар — после
обмена файлами у обоих будет всё.

## Что на экранах

**Дашборд** — показатели за период с сравнением к предыдущему такой же длины:
отдельно общее по предприятию, по кухне и по бару. Ниже доли кухни и бара по
выручке, прибыли и количеству, динамика по неделям, сравнение кухни с баром и
топ позиций по количеству и по прибыли.

**Категории** — та же таблица, что вы вели руками: строки категорий и позиций,
колонки недель, справа итог. Категория разворачивается в позиции. Сортировка
раздельная: одной парой кнопок сортируются категории, другой — позиции внутри
них; у каждой свой порядок и своё направление.

При показателях **«Наценка»** и **«Себестоимость»** справа от названия
появляется колонка **«База»** — ориентир, который шеф вписывает сам, чтобы
сравнивать с ним недельные значения. Значение можно менять и стирать (очистить
поле), на расчёты оно не влияет. У наценки и себестоимости базы независимые,
задаются и для категории целиком, и для отдельной позиции. Хранятся вместе с
базой и уезжают к коллегам вместе с ней.

**Данные** — загрузка выгрузок, журнал загрузки, список загруженных недель,
банкеты вручную, справочник групп бара, экспорт и импорт базы.

В списке недель кухня и бар удаляются по отдельности: нажмите «Кухня» или
«Бар» в нужной строке, чтобы убрать только этот раздел, или «Удалить всё» —
чтобы стереть неделю целиком. Перезаливать перед этим не обязательно: загрузка
файла и так заменяет свой раздел за свою неделю.

Фильтры сверху: период (включая «Последняя неделя», «Предыдущая неделя» и свой
диапазон), раздел, группа бара, категории.

## Группы бара

АЛКОГОЛЬ, БОКАЛЫ, КОКТЕЙЛИ, БУТЫЛКИ, Б/А в выгрузке отсутствуют — они задаются
на вкладке «Данные». Состав перенесён из вашего старого отчёта; изменение
применяется сразу ко всем неделям, включая прошлые. По группе можно
фильтровать весь дашборд.

## Что отчёт делает с данными сам

**Наценка не усредняется по позициям** — считается от сумм периода:
прибыль ÷ себестоимость, где себестоимость = выручка − валовая прибыль. Точно
так же считает строка «Итого» в выгрузке, поэтому цифры совпадают до десятых.

Если число на дашборде не сходится с вашей таблицей, первым делом посмотрите
на период вверху: по умолчанию показаны последние 8 недель, а в таблице обычно
одна неделя. Выберите «Последняя неделя» — и цифры совпадут.

**Строки, которых нет в выгрузке.** Иногда система печатает «Итого» по
категории, но не все строки под ним. Разница сохраняется позицией
«(без детализации)» — без этого бар за 17.08 терял 52 штуки и 27 тыс. ₽.
В неделях из старого отчёта эта строка крупная: там по бару детально велись
только сеты, коктейли и бранчи, остальное лежало в итоге категории.

**Откуда данные.** Только настоящие выгрузки из учётной системы, по обоим
разделам, с 4 мая. Всё, что раньше переносилось из ручного отчёта, удалено:
там встречались незаполненные «Итого» по категориям и, судя по цифрам, сдвиг
бара на неделю. Теперь каждая неделя сходится с выгрузкой до сотых.

Недели раньше 4 мая отчёт не ведёт: они отбрасываются при открытии, даже если
остались в браузере с прошлых версий файла или пришли из чужой базы. Чтобы
вернуть их, нужны настоящие выгрузки за январь–апрель — пришлите, и я подставлю
(дата отсечки задаётся в `build.mjs`).

**Предупреждение о неполной выгрузке.** Если в загруженном файле итог по
категории больше суммы напечатанных строк, журнал загрузки скажет об этом:
какая категория и сколько не хватает. Это сигнал выгрузить неделю из учётной
системы заново — тогда строка «(без детализации)» исчезнет сама.

Сейчас во всех загруженных выгрузках расхождений нет.

**Прочерк вместо нуля.** Если данных о позиции за неделю нет, в клетке стоит
«—». Раньше там был «0 ₽», и выглядело это как «не продавали», хотя на деле
сведений просто не было.

**Переименования.** Позиции сопоставляются по схожести названий: если блюдо
переименовали, история не разрывается. Порог задаётся в настройках компонента
(по умолчанию 90%).

**Неполные недели.** Если за неделю загружен только один раздел, отчёт
предупреждает и считает по тому, что есть, а не нулём.

**Повторная загрузка недели** заменяет данные, а не удваивает.

## Осталось решить

1. **Бар в истории, похоже, сдвинут на неделю.** В старом отчёте столбец
   «24-30» по бару содержит 667 штук — ровно столько, сколько в выгрузке за
   17–23 августа. По кухне сдвига нет. Если это так, историю нужно
   переимпортировать со сдвигом; либо загрузите настоящие выгрузки поверх.
2. **Позиции с наценкой −100%** (комплименты, списания) входят в общие цифры
   и тянут наценку вниз. Выносить отдельно?
3. **Столбец «срез»** в старом отчёте везде пустой — что в него должно попадать?
4. **За неделю 10–16.08 нет выгрузки по бару** — потерялась или не делали?

---

## Для тех, кто правит код

`отчет.html` собирается из исходников, руками его не редактируют.

```bash
node build.mjs    # собрать отчет.html
node test.mjs     # проверки (нужен: npm i jsdom)
```

| Файл | Что внутри |
|---|---|
| `src/template.html` | разметка экранов |
| `src/app.js` | вся логика: разбор выгрузок, база, агрегации, экраны |
| `src/xlsxread.js` | чтение .xlsx: распаковка ZIP и разбор XML, без библиотек |
| `src/xlsx-shim.js` | обёртка с интерфейсом SheetJS поверх своего читателя |
| `src/boot.js` | передача шаблона рантайму текстом (см. ниже) |
| `src/props.txt` | настройки компонента |
| `vendor/` | React 18 и рантайм Claude Design |
| `data/history.json` | история 2026 года из ручного отчёта |
| `data/exports/` | настоящие выгрузки; перекрывают ручной отчёт по своим неделям |
| `build.mjs`, `test.mjs` | сборка и проверки |

Три места, где пришлось отойти от исходного документа дизайна:

- **React встроен в файл.** Рантайм грузил его с CDN — теперь берёт готовый.
- **Чтение Excel своё.** Вместо библиотеки с CDN — собственная распаковка,
  обёрнутая в тот же интерфейс `XLSX.read` / `sheet_to_json`.
- **Шаблон передаётся рантайму текстом** (`src/boot.js`). HTML-парсер
  выбрасывает `<sc-for>` внутри `<select>` — там разрешены только `<option>`,
  из-за чего выпадающие списки оставались пустыми.

Ещё база готовится в конструкторе, а не в `componentDidMount`: списки в
шаблоне собираются при первом рендере и потом не пересобираются.
